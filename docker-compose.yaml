services:
  # =============================================================================
  # PostgreSQL Database - Configuração otimizada para produção
  # =============================================================================
  postgresql:
    image: postgres:17-alpine
    container_name: postgres_container
    
    # 🔒 PRESERVADO: Volume existente para manter dados
    volumes:
      - db:/var/lib/postgresql/data
      - ./postgres-logs:/var/log/postgresql
    
    ports:
      - "5432:5432"
    
    # 🔒 PRESERVADO: Configurações de ambiente existentes
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=adminpassword
      - POSTGRES_DB=controle_pedidos
      # ⚡ CORRIGIDO: Manter configurações existentes para evitar warnings
      - POSTGRES_INITDB_ARGS=--encoding=UTF-8 --lc-collate=en_US.utf8 --lc-ctype=en_US.utf8 --data-checksums
      - POSTGRES_HOST_AUTH_METHOD=md5
      # ⚡ NOVO: Configurações de locale consistentes
      - LC_ALL=en_US.utf8
      - LANG=en_US.utf8
    
    # 🔒 PRESERVADO: Rede existente
    networks:
      - controle_pedidos_network
    
    # ⚡ OTIMIZADO: Resource limits para Coolify (2 vCPUs / 4GB RAM)
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1.5G
        reservations:
          cpus: '0.5'
          memory: 512M
    
    # ⚡ OTIMIZADO: Health check menos agressivo
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin -d controle_pedidos -h localhost"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 30s
    
    # ⚡ OTIMIZADO: Restart policy
    restart: unless-stopped
    
    # 🔒 Security context
    security_opt:
      - no-new-privileges:true
    
    # 📊 Logging estruturado
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
        labels: "service=postgresql"

  # =============================================================================
  # API Application - Configuração otimizada para Node.js
  # =============================================================================
  api-um-doce:
    build:
      context: .
      dockerfile: Dockerfile
      # Cache otimizado
      cache_from:
        - node:22.15-alpine3.20
    
    container_name: api_um_doce_container
    
    # 🔗 Dependência com health check
    depends_on:
      postgresql:
        condition: service_healthy
    
    ports:
      - "3333:3333"
    
    # 🔒 PRESERVADO: Todas as variáveis de ambiente existentes
    environment:
      - PORT=3333
      - DB_HOST=postgresql
      - DB_PORT=5432
      - DB_USER=admin
      - DB_PASSWORD=adminpassword
      - DB_NAME=controle_pedidos
      - DATABASE_URL=postgresql://admin:adminpassword@postgresql:5432/controle_pedidos
      - NODE_ENV=production
      # ⚡ NOVO: Otimizações Node.js
      - NODE_OPTIONS=--max-old-space-size=2048 --enable-source-maps
      - TZ=America/Sao_Paulo
      - NPM_CONFIG_LOGLEVEL=warn
      # ⚡ NOVO: Configurações para timeout em produção
      - HTTP_TIMEOUT=30000
      - KEEP_ALIVE_TIMEOUT=65000
    
    # 🔒 PRESERVADO: Rede existente
    networks:
      - controle_pedidos_network
    
    # ⚡ OTIMIZADO: Resource limits para aplicação Node.js
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 2G
        reservations:
          cpus: '0.25'
          memory: 256M
    
    # ⚡ OTIMIZADO: Health check menos frequente para produção
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://localhost:3333/health', (res) => { process.exit(res.statusCode === 200 ? 0 : 1) }).on('error', () => process.exit(1))\""]
      interval: 30s
      timeout: 15s
      retries: 3
      start_period: 60s
    
    # ⚡ OTIMIZADO: Restart policy com delay
    restart: unless-stopped
    
    # 🔒 Security context
    security_opt:
      - no-new-privileges:true
    read_only: false
    
    # 📊 Logging estruturado
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "5"
        labels: "service=api-um-doce"
    
    # 🚀 Labels para Coolify/Traefik
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.api-um-doce.rule=Host(`api.umdoce.dev.br`)"
      - "traefik.http.services.api-um-doce.loadbalancer.server.port=3333"
      - "traefik.http.routers.api-um-doce.tls=true"
      - "traefik.http.routers.api-um-doce.tls.certresolver=letsencrypt"
      - "coolify.managed=true"

# =============================================================================
# Network Configuration - PRESERVADA configuração existente
# =============================================================================
networks:
  controle_pedidos_network:
    name: controle_pedidos_network
    driver: bridge

# =============================================================================
# Volumes Configuration - PRESERVADO volume existente
# =============================================================================
volumes:
  # 🔒 PRESERVADO: Volume principal do banco de dados
  db:
    driver: local
  
  # ⚡ NOVO: Volume para logs do PostgreSQL
  postgres-logs:
    driver: local